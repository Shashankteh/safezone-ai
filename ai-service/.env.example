PORT=8000
NODE_ENV=development

# JWT (same secret as Node.js backend for token verification)
JWT_SECRET=your_super_secret_jwt_key_min_32_chars

# Allow requests from Node backend only
ALLOWED_ORIGINS=http://localhost:5000
